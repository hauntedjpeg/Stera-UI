---
title: Dialog
description: A modal window that overlays the page to display focused content or capture user input.
---

<ComponentPreview name="dialog-demo" />

## Installation

<InstallSnippet slug="dialog" hideHeading />

## Confirmation Dialog

<ComponentPreview name="dialog-confirm" />

Use `DialogClose` with a custom button to create cancel/confirm action pairs.

## Scrollable Content

<ComponentPreview name="dialog-scrollable" />

Set `scroll="content"` on `DialogPopup` and `scroll` on `DialogContent` to let the body scroll while the header and footer remain sticky.

## API Reference

### DialogPopup

| Prop | Type | Default |
| --- | --- | --- |
| `scroll` | `false \| "content"` | `false` |
| `showCloseButton` | `boolean` | `true` |
| `initialFocus` | `boolean` | `true` |

### DialogFooter

| Prop | Type | Default |
| --- | --- | --- |
| `showCloseButton` | `boolean` | `false` |

### DialogTrigger

| Prop | Type | Description |
| --- | --- | --- |
| `render` | `ReactElement` | Render prop for the trigger element |

### DialogClose

| Prop | Type | Description |
| --- | --- | --- |
| `render` | `ReactElement` | Render prop for the close element |
