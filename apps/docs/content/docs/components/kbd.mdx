---
title: Kbd
description: Keyboard shortcut display components for showing key combinations in UI.
---

<ComponentPreview name="kbd-demo" caption="Kbd" />

## Installation

<InstallSnippet slug="kbd" hideHeading />

## Composition

Use the following composition to build `Kbd` and `KbdGroup`:

```text
Kbd
KbdGroup
├── Kbd
└── Kbd
```

## Examples

### Sizes

<ComponentPreview name="kbd-sizes" caption="Kbd Sizes" />

| Size | Description |
| --- | --- |
| `sm` | Compact, for tight spaces like tooltips |
| `md` | Default |

### Key Combinations

Use `KbdGroup` to display a sequence of keys.

<ComponentPreview name="kbd-key-combination" caption="Kbd Combo" />

### Inside a Button

Use `data-kbd="inline-end"` to position a `Kbd` at the trailing edge of a button.

<ComponentPreview name="kbd-with-button" caption="Kbd with Button" />

### Inside an Input

<ComponentPreview name="kbd-with-input" caption="Kbd with Input" />

## API Reference

### Kbd

| Prop | Type | Default |
| --- | --- | --- |
| `size` | `"sm" \| "md"` | `"md"` |

Renders a `<kbd>` element. Accepts all standard `<kbd>` props.

### KbdGroup

Renders a `<kbd>` wrapper for grouping multiple `Kbd` components. Accepts all standard `<div>` props.
